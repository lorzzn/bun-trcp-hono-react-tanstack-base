{
  "name": "server",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "dev": "bun run --hot index.ts",
    "build": "bun build index.ts --outdir dist --target bun",
    "initdb": "prisma migrate dev --name init"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@prisma/client": "^6.16.2",
    "@trpc/server": "^11.6.0",
    "hono": "^4.9.8",
    "prisma": "^6.16.2",
    "prisma-trpc-generator": "^2.4.0",
    "prisma-trpc-shield-generator": "^0.2.1",
    "prisma-zod-generator": "^1.21.4",
    "trpc-shield": "^1.3.0",
    "zod": "3"
  }
}
